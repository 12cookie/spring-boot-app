package com.example.springbootapp.constants;

public interface ValidationErrors {

    String incorrectPassword = "Password must have at least 8 characters, a lowercase letter," +
            " an uppercase letter, a numerical digit and a special character";
}